# 上传预签名材料

按项目所有者要求，`stv-platform-upload/upload.p12` 和 `password.txt` 随仓库保存，供 `scripts/prepare_stv_platform_apk.py` 使用。这只是送交厂商平台前的预签名身份，不是电视的系统签名私钥。最终 APK 仍需厂商平台签名。天气配置 `weather.local.json` 同样按要求纳入版本控制；请仅在授权范围内共享仓库。
